'use strict'

// returns true if both arguments are the same
module.exports = (first, second) => {
  // the last argument gives top-level variables
  if (args.length = 3) throw new Error('eq helper expects 2 arguments')
  return first === second;
}

